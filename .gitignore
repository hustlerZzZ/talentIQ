# Node.js
node_modules/
npm-debug.log
yarn-error.log

# React build files
build/
dist/

# .env files for environment variables (any subfolder)
.env
.env.local
.env.development
.env.test
.env.production

# Next.js specific (if using Next.js)
.next/
out/

# VSCode settings (any subfolder)
.vscode/

# MacOS system files
.DS_Store

# Linux system files
*~

# Windows system files
Thumbs.db

# Log files (any subfolder)
*.log

# Package lock and yarn lock files (optional, include if necessary)
package-lock.json
yarn.lock

# TypeScript (if using TypeScript)
*.tsbuildinfo

# Coverage directory (if using code coverage tools like Jest)
coverage/

# Docker files (if using Docker)
.docker/

# Test output directories (Jest, Mocha, etc.)
test-coverage/

# Other IDE/editor directories (e.g., JetBrains IDEs)
.idea/

# Build artifacts (if using other build tools)
out/

# Ignore all files in subdirectories called "tmp" (example)
tmp/

# Ignore files and directories with a specific extension in any subfolder (e.g., .bak files)
*.bak

# Ignore all .log files in any folder
**/*.log
